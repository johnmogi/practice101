let light = false;

function switchLight(){
    light = !light
    // if (!light) return light = true;
    // if ( light) return light = false;
}

switchLight();
// switchLight();

console.log(light);